#include <stdio.h>

/**
 * main - this is the main functions
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int x;

	for (x = 0 ; x < 10 ; x++)
		printf("%d", x);
	printf("\n");
	return (0);
}
